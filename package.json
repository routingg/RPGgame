{
  "name": "rotting-world",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "node server.mjs", "check": "node --check dist/app.js && node --check dist/game-core.js && node --check dist/bubble-core.js", "test": "node --test tests/*.test.mjs" }
}
